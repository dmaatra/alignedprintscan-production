# Patch 18.8.2 — Dashboard Async Syntax Hotfix

Fixes the dashboard JavaScript syntax/runtime error caused by a standalone `async` token before `detailMap()`.

Changed file:
- assets/js/admin.js

No Supabase function redeploy needed. Commit/push and hard refresh the dashboard.
